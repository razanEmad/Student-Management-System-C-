How to run:

Open DatabaseScript.sql in SQL Server and execute it to create the database.

Open the solution in Visual Studio.

Check App.config or DBHelper.cs to ensure the connection string matches your SQL Server name.

Run the project.
